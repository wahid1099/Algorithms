// #include<iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        int coins[n];
        for (int i = 0; i < n; i++)
        {
            cin >> coins[i];
        }

        int max = coins[0];
        int maxsum = 0;
        for (int i = 0; i < n; i++)
        {
            coins[i];
        }
    }

    return 0;
}